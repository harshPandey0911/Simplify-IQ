# Local Development URL (default)
# In Vercel, this can be left blank or set to the production URL, 
# although relative paths will work automatically with the vercel.json rewrites.
VITE_API_URL=http://localhost:5050
